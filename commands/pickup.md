---
description: Pick up where the last session left off after clearing context — read backlog.md and changelog.md, report the current task and what last shipped, then wait.
---

Run **Phase 1 (Start / orient)** of the `session-backlog` skill — this is the post-`/clear` counterpart to `/handoff`.

Follow the skill's Phase 1 steps:

1. Read `backlog.md` and `changelog.md` from the repository root (check both `backlog.md`/`BACKLOG.md` casings).
2. Report the current task — the first item in `backlog.md`. Read its body, not just the header, and summarise in one sentence.
3. Report what last shipped — the top entry in `changelog.md`, one sentence.
4. If `backlog.md` is empty, ask me what to work on. Do not pick a task on your own.

**Then stop and wait.** Do not begin the task until I explicitly confirm. Once I do, work only on the first backlog item.
